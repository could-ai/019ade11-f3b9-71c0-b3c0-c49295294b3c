name: couldai_user_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Game Engine
  flame: ^1.16.0
  # State Management
  provider: ^6.1.1
  # Local Storage
  shared_preferences: ^2.2.2
  # Audio (Flame has built-in audio, but we will use basic audio wrappers)
  flame_audio: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
